{
    "layer": "top",
    "margin": 0,
    "position": "top",
    "modules-left": ["river/tags", "hyprland/workspaces"],
    "modules-center": ["river/window", "hyprland/window", "niri/window"],
    "modules-right": ["pulseaudio", "memory", "network", "tray", "clock"],

    "river/tags": {
        "num-tags": 11,
        "tag-labels": ["1", "2", "3", "4", "5", "6", "7", "8", "9", "Q", "W"],
        "format": "{name}fd{if:occupied:●}"
    },

    "hyprland/workspaces": {
        "disable-scroll": true,
    },

    "niri/workspaces": {
        "format": "{icon}"
    },

    "tray": {
        "icon-size": 20,
        "spacing": 4
    },
    "clock": {
        "tooltip-format": "{:%Y-%m-%d | %H:%M}",
        "format-alt": "{:%Y-%m-%d}"
    },

    "network": {
        // "interface": "wlp2*", // (Optional) To force the use of this interface
        "format-wifi": " ",
        "format-ethernet": "󰛳 ",
        "format-linked": "{ifname} 󰱟 ",
        "format-disconnected": "󰱟 ",
        "format-alt": "{ifname}: {ipaddr}/{cidr}",
        "tooltip-format-wifi": "{essid} ({signalStrength}%)  ",
        "tooltip-format-ethernet": "{ifname}: {ipaddr}/{cidr} 󰛳 "
    },

    "pulseaudio": {
        "format": "{icon}",
        "format-bluetooth": "{volume}% {icon} {format_source}",
        "format-muted": "󰖁 {format_source}",
        "format-icons": {
            "headphones": " ",
            "default": ["", "", "󰕾"]
        },
        "on-click": "pavucontrol"
    }
}
